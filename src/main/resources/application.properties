spring.application.name=workoutTracker

# Server Port
server.port=8080

# Logging
logging.level.root=WARN
logging.level.web=INFO

logging.level.com.hanjing.workoutTracker=DEBUG

# Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/workouttracker?allowPublicKeyRetrieval=true&useSSL=false&createDatabaseIfNotExist=true
spring.datasource.username=root
# Ensure this matches your actual MySQL root password
spring.datasource.password=YourNewPassword123!

# JPA / Hibernate
spring.jpa.generate-ddl=true
# Optional: Useful for debugging SQL queries in the console
spring.jpa.show-sql=true
spring.jpa.hibernate.ddl-auto=update

# Security Constants
token.expirationms=3600000
# NOTE: 3,600,000 ms is 1 hour. Your previous value (360,000) was only 6 minutes.
token.secret.key=eb802de041eeb5da1b729a9b77861c057a27238cd6d243fbe344de3be10472af